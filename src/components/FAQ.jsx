import {useState} from 'react';import {Plus} from 'lucide-react';import {faqs} from '../data/content';export default function FAQ(){const[open,setOpen]=useState(0);return <section className="section faq" id="faq"><div className="container faq-grid"><div className="section-heading reveal"><span className="eyebrow">QUESTIONS, ANSWERED</span><h2>Everything you need to know.</h2><p>Still curious? Our product team would be happy to help.</p><button className="text-btn" onClick={()=>document.querySelector('#cta')?.scrollIntoView({behavior:'smooth'})}>Talk to our team <span>→</span></button></div><div className="accordion reveal">{faqs.map(([q,a],i)=><div className={`faq-item ${open===i?'open':''}`} key={q}><button onClick={()=>setOpen(open===i?-1:i)} aria-expanded={open===i} aria-controls={`faq-${i}`}><span>{q}</span><Plus size={19}/></button><div id={`faq-${i}`} className="faq-answer" role="region"><p>{a}</p></div></div>)}</div></div></section>}
