import {steps} from '../data/content';export default function HowItWorks(){return <section className="section how" id="how-it-works"><div className="container"><div className="how-head reveal"><div><span className="eyebrow">HOW IT WORKS</span><h2>From busywork to momentum in four steps.</h2></div><p>NOVA turns the complexity of modern teamwork into a workflow your team can actually enjoy using.</p></div><div className="steps">{steps.map(([n,t,d],i)=><article className="step reveal" key={n}><div className="step-number">{n}</div>{i<steps.length-1&&<span className="step-line"/>}<h3>{t}</h3><p>{d}</p></article>)}</div></div></section>}
