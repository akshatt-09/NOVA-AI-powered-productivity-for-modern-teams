import SectionHeading from './SectionHeading';import FeatureCard from './FeatureCard';import {features} from '../data/content';export default function Features(){return <section className="section" id="features"><div className="container"><SectionHeading eyebrow="ONE WORKSPACE. MORE MOMENTUM." title="Everything your team needs to move faster." text="From the first task to the final report, NOVA keeps execution focused, measurable and beautifully simple." center/><div className="features-grid">{features.map(f=><FeatureCard key={f.title}{...f}/>)}</div></div></section>}
