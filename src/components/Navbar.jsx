import {useState} from 'react';import {Menu,X,Sun,Moon,ArrowUpRight} from 'lucide-react';import {navLinks} from '../data/content';
export default function Navbar({theme,onToggle}){const[open,setOpen]=useState(false);const go=(href)=>{setOpen(false);document.querySelector(href)?.scrollIntoView({behavior:'smooth'});};return <header className="navbar"><div className="container nav-inner"><a className="brand" href="#top" onClick={()=>setOpen(false)}><span className="brand-mark">N</span>NOVA</a><nav className={`nav-links ${open?'open':''}`} aria-label="Primary navigation">{navLinks.map(([label,href])=><button key={href} onClick={()=>go(href)}>{label}</button>)}<button className="mobile-sign" onClick={()=>go('#cta')}>Get Started <ArrowUpRight size={16}/></button></nav><div className="nav-actions"><button className="theme-btn" onClick={onToggle} aria-label={`Switch to ${theme==='light'?'dark':'light'} theme`}>{theme==='light'?<Moon size={18}/>:<Sun size={18}/>}</button><button className="sign-in" onClick={()=>go('#cta')}>Sign In</button><button className="nav-cta" onClick={()=>go('#cta')}>Get Started <ArrowUpRight size={16}/></button><button className="menu-btn" onClick={()=>setOpen(!open)} aria-label={open?'Close menu':'Open menu'} aria-expanded={open}>{open?<X/>:<Menu/>}</button></div></div></header>}
