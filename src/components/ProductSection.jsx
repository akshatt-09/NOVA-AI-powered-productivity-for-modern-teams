import {Check,ArrowRight} from 'lucide-react';import Dashboard from './Dashboard';export default function ProductSection(){return <section className="section product-section"><div className="container product-grid"><div className="product-copy reveal"><span className="eyebrow">A CALMER WAY TO OPERATE</span><h2>Your entire workflow, with intelligence built in.</h2><p>NOVA gives every team a shared view of what matters, while AI takes care of the busywork underneath.</p><ul>{['Know what needs attention before it becomes a blocker.','Automate repetitive workflows without losing control.','Give every teammate context without more meetings.'].map(x=><li key={x}><span><Check size={15}/></span>{x}</li>)}</ul><button className="text-btn" onClick={()=>document.querySelector('#how-it-works')?.scrollIntoView({behavior:'smooth'})}>See how NOVA works <ArrowRight size={17}/></button></div><div className="product-visual reveal"><Dashboard large/></div></div></section>}
